import api from "../api";

export default {
  sendLoginData(login, password) {
    if (login !== null && password !== null) {
      console.log(login, password);
      return "REASDF423424FSDF";
    }
  },
};
